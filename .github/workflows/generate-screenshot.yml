name: Dynamic Capture Engine

on:
  repository_dispatch:
    types: [capture_summary_table] # Listens for the ping from your website HTML
  workflow_dispatch: # Allows you to also run it manually from GitHub actions UI for testing

jobs:
  screenshot:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Install Puppeteer Browser Dependencies
      run: npm install puppeteer

    - name: Run Snapshot Capture Script
      env:
        # Maps the repository dispatch payload, fallback to empty string if missing
        BATTLE_IDS: ${{ github.event.client_payload.ids || '' }}
      run: node snap-alliance.js

    - name: Commit and Push Image Asset
      run: |
        git config --global user.name "GitHub Actions Automation"
        git config --global user.email "actions@github.com"
        git add battle-images/*.png
        git commit -m "Automated System Update: Generated battle summary image [skip ci]" || exit 0
        git push